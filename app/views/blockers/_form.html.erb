<%= form_for @blocker do |form| %>

<% if @blocker.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@blocker.errors.count, "error") %> prohibited
    this blocker from being saved:</h2>
  <ul>
  <% @blocker.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="form-group">
      <p>
        <div class="text-center">
        <%= form.label :title%><br/>
      </div>
        <%= form.text_field :title, class: "form-control text-center" %>
      </p>

      <p>
        <div class="text-center">
        <%= form.label :description%><br/>
      </div>
        <%= form.text_area :description, class: "form-control text-center" %>
      </p>
      <div class="text-center">
          <%= form.submit :class => "btn btn-default" %>
      </div>
    </div>
  </div>
</div>
<% end %>
